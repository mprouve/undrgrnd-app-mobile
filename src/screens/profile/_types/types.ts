import type {
  AuthenticatedStackScreenProps,
  AuthenticatedStackRouteProps
} from '../../../navigation/authenticated/_types/types'

export type ProfileScreenNavigationProp = AuthenticatedStackScreenProps<'profile'>
export type ProfileScreenRouteProp = AuthenticatedStackRouteProps<'profile'>
