import { createNativeStackNavigator } from '@react-navigation/native-stack'
import { ForgotPasswordStackParamList } from '../_types/types'

const ForgotPasswordStack = createNativeStackNavigator<ForgotPasswordStackParamList>()

export default ForgotPasswordStack
